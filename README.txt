To compile zassemble, use the following command:
    gcc zassemble.c -o zassemble -std=c99 -w

To run zassemble, use this command:
    ./zassemble myfile.s [machine_code.coe] [data.coe]

The arguments in square brackets [] are optional, and will default to the names
shown above.
